""" Contains the Never Follow heuristic."""


def never_follow(params: dict = {}):
    """Simple strategy that performs no migration decision regardless users' mobility.

    Args:
        params (dict, optional): User-defined parameters. Defaults to {}.
    """
    ...
